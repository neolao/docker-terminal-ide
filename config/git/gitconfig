[color]
    diff = auto
    status = auto
    branch = auto
    ui = auto

[push]
    default = simple

[core]
    editor = nvim
    pager = less -R

[include]
    path = /git/config/aliases
    path = /var/ide/git/aliases
    path = /var/ide/git/user
